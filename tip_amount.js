// The tip on a $some-number food bill is $some-number
let foodPrice= 33.00;

let tipAmount= foodPrice *.2;
// console.log(tipAmount);

let totalAmount= foodPrice + tipAmount;

console.log(totalAmount);

console.log(`Tip on the$ ${foodPrice.toFixed(2)} food bill is $${tipAmount.toFixed(2)}`)

